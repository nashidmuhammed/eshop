/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['127.0.0.1','api.nfour.jesyfoods.com'], // Add any other domains you need here
  },

};

export default nextConfig;